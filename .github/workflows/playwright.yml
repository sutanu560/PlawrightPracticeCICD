name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    # Runs every 5 minutes (polls main/master to mirror Jenkins' H/5 pollSCM)
    - cron: '*/5 * * * *'

env:
  PLAYWRIGHT_BROWSERS_PATH: '0'

jobs:
  test:
    name: Run Playwright CI
    timeout-minutes: 30
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'

    - name: Install Dependencies
      run: npm ci

    - name: Install Playwright Browsers (Chromium only)
      run: npx playwright install chromium --with-deps

    - name: Run Playwright Tests
      run: npx playwright test --project=chromium

    - name: Upload Playwright HTML Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 10

    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: test-results/
        retention-days: 10

    # GitHub natively sends email notifications on build failures if configured in repository settings.
    # Below is a manual step for custom reporting parity with the emailext plugin, commented out.
    # Uncomment and configure MAIL_USERNAME and MAIL_PASSWORD in GitHub Secrets to use it.
    # 
    # - name: Send Email Notification
    #   if: always()
    #   uses: dawidd6/action-send-mail@v3
    #   with:
    #     server_address: smtp.gmail.com
    #     server_port: 465
    #     secure: true
    #     username: ${{ secrets.MAIL_USERNAME }}
    #     password: ${{ secrets.MAIL_PASSWORD }}
    #     subject: '[${{ job.status }}] Playwright Tests - ${{ github.workflow }} #${{ github.run_number }}'
    #     to: sutanu560@gmail.com
    #     from: GitHub Actions CI
    #     html_body: |
    #       <p>Build result: <strong>${{ job.status }}</strong></p>
    #       <p><a href="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}">Build #${{ github.run_number }}</a></p>
    #       <p>Triggered by <b>${{ github.ref_name }}</b>.</p>
    #     ignore_cert: true
